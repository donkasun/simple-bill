VITE_API_KEY=
VITE_AUTH_DOMAIN=
VITE_PROJECT_ID=
VITE_STORAGE_BUCKET=
VITE_MESSAGING_SENDER_ID=
VITE_APP_ID=

# Dev-only: set in .env.development.local (gitignored) to skip Firebase
# auth and load a mock user — useful for Playwright / local UI testing.
# VITE_MOCK_USER=true
